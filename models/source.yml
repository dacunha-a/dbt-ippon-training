version: 2

sources:
  - name: source
    database: DB_DBT_ADACUNHA_DEV
    schema: source
    
    tables:
      - name: restaurants
        columns:
        - name: open_on_sunday
        - name: address
          tests:
            - not_null  
      - name: orders
        columns:
        - name: identifier
          tests:
          - unique
        - name: restaurant_identifier
      - name: dishes
